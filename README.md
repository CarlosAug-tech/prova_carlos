<h1 align="center" >
  Prova para Estágio
</h1>

## 📦 Como baixar o projeto

```bash

  # Clonar o repositório
  $ git clone https://github.com/CarlosAug-tech/prova_carlos.git

  # --- Backend ---

  # Entrar no diretório do backend
  $ cd backend

  # Modificar as variáveis ambientes (criar um arquivo .env)

  # Preencher com os dados do .env.example como referência

  # Instalar as dependências
  $ yarn install

  # Iniciar o backend
  $ yarn dev

   # --- Frontend ---

  # Entrar no diretório do frontend
  $ cd frontend

  # Modificar as variáveis ambientes (criar um arquivo .env)

  # Preencher com os dados do .env.example como referência

  # Instalar as dependências
  $ yarn install

  # Iniciar o frontend
  $ yarn start

```

---
